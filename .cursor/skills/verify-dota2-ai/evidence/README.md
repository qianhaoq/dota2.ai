# Evidence (local only)

Proof artifacts written by `verify-dota2-ai drive` land here:

```
evidence/<feature-id>/<runId>/
  landing.png
  after.png
  page.txt
  aria.txt
  meta.json
```

This directory is gitignored except this README. Cleanup tears down processes and `.run/` scratch only — it never deletes these files.
